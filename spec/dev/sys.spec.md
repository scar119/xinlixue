# 系统架构设计文档
**项目**: 心理学-AI工具站点
**版本**: v1.0
**日期**: 2025-01-28
**状态**: 草案

---

## 一、架构概述

### 1.1 系统定位

**类型**: 全栈 Web 应用 (SSR + CSR)
**架构模式**: 三层架构 + 微服务预备
**部署方式**: 容器化 + CDN

### 1.2 架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ 浏览器   │  │ 移动端   │  │ SEO爬虫  │  │ 第三方   │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────────┘
                            ↓ HTTPS
┌─────────────────────────────────────────────────────────────┐
│                       CDN 层 (EdgeOne)                      │
│                    静态资源 + DDoS防护                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                     Next.js 应用层                          │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ SSR页面  │  │ API路由  │  │Server AC │  │中间件    │  │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────────────────────┘
        ↓                              ↓
┌──────────────┐            ┌──────────────────┐
│  PostgreSQL  │            │   外部服务       │
│   (Neon)     │            │  ┌──────────┐    │
│              │            │  │ OpenAI   │    │
│  ┌────────┐  │            │  │ Google   │    │
│  │  数据  │  │            │  │ Analytics│    │
│  │        │  │            │  └──────────┘    │
│  └────────┘  │            └──────────────────┘
└──────────────┘
```

---

## 二、技术栈

### 2.1 前端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Next.js** | 14+ | 全栈框架 |
| **React** | 18+ | UI 库 |
| **TypeScript** | 5.0+ | 类型安全 |
| **Tailwind CSS** | 3.4+ | 样式框架 |
| **shadcn/ui** | latest | UI 组件库 |
| **Zustand** | latest | 状态管理 |
| **React Query** | latest | 数据获取 |
| **Zod** | latest | 数据验证 |
| **next-seo** | latest | SEO 优化 |

### 2.2 后端技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **Next.js API** | 14+ | API 路由 |
| **Server Actions** | latest | 服务端操作 |
| **Drizzle ORM** | latest | 数据库 ORM |
| **Zod** | latest | 请求验证 |
| **Bcrypt** | latest | 密码加密 |
| **Jose** | latest | JWT 处理 |

### 2.3 数据库技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **PostgreSQL** | 17+ | 主数据库 |
| **Neon** | latest | 云数据库服务 |
| **Drizzle Kit** | latest | 迁移工具 |

### 2.4 AI/ML 技术栈

| 技术 | 版本 | 用途 |
|------|------|------|
| **OpenAI API** | GPT-4 | 内容生成 |
| **Vercel AI SDK** | latest | AI 集成 |
| **LangChain** | latest | Prompt 管理 (可选) |

### 2.5 DevOps 工具

| 工具 | 版本 | 用途 |
|------|------|------|
| **Bun** | latest | 包管理/运行时 |
| **ESLint** | latest | 代码检查 |
| **Prettier** | latest | 代码格式化 |
| **TypeScript** | 5.0+ | 类型检查 |
| **EdgeOne** | latest | CDN/部署 |

---

## 三、前端架构

### 3.1 目录结构

```
src/
├── app/                    # Next.js App Router
│   ├── (auth)/            # 认证相关页面
│   │   ├── login/
│   │   └── register/
│   ├── (main)/            # 主要应用页面
│   │   ├── kuakua/        # 夸夸功能
│   │   ├── tests/         # 测试功能
│   │   ├── theories/      # 理论知识
│   │   ├── games/         # 游戏功能
│   │   └── profile/       # 个人中心
│   ├── api/               # API 路由
│   │   ├── v1/
│   │   │   ├── kuakua/
│   │   │   ├── tests/
│   │   │   └── users/
│   │   └── auth/
│   ├── layout.tsx         # 根布局
│   └── page.tsx           # 首页
├── components/            # React 组件
│   ├── ui/               # shadcn/ui 组件
│   ├── kuakua/           # 夸夸相关组件
│   ├── tests/            # 测试相关组件
│   └── shared/           # 共享组件
├── lib/                  # 工具函数
│   ├── ai/               # AI 相关
│   ├── db/               # 数据库相关
│   ├── auth/             # 认证相关
│   └── utils.ts          # 通用工具
├── hooks/                # 自定义 Hooks
├── stores/               # Zustand 状态
├── types/                # TypeScript 类型
└── styles/               # 全局样式
```

### 3.2 状态管理架构

```
┌─────────────────────────────────────────────┐
│              Zustand Stores                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ authStore│  │ userStore│  │ uiStore  │  │
│  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│            React Query (Server State)        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│  │ kuakua   │  │ tests    │  │ theories │  │
│  └──────────┘  └──────────┘  └──────────┘  │
└─────────────────────────────────────────────┘
```

### 3.3 组件设计模式

**原子设计模式**:
```
atoms/     # 基础组件 (Button, Input)
  ↓
molecules/ # 分子组件 (FormField, Card)
  ↓
organisms/ # 组织组件 (TestCard, UserProfile)
  ↓
templates/ # 页面模板 (TestPage, ProfilePage)
  ↓
pages/     # 完整页面
```

---

## 四、后端架构

### 4.1 API 架构

**RESTful API 设计**:
```
/api/v1/
├── auth/
│   ├── POST    /register     # 注册
│   ├── POST    /login        # 登录
│   └── POST    /logout       # 登出
├── kuakua/
│   ├── POST    /             # 创建夸夸
│   ├── GET     /history      # 获取历史
│   └── POST    /save         # 保存记录
├── tests/
│   ├── GET     /             # 获取测试列表
│   ├── GET     /:id          # 获取测试详情
│   ├── POST    /:id/take     # 提交答案
│   └── GET     /:id/result   # 获取结果
├── users/
│   ├── GET     /me           # 获取当前用户
│   ├── PATCH  /me           # 更新用户信息
│   └── DELETE /me           # 删除账户
└── admin/
    ├── GET     /stats        # 统计数据
    └── GET     /users        # 用户管理
```

### 4.2 Server Actions 架构

```typescript
// app/actions/kuakua.ts
'use server'

export async function createKuakua(input: string) {
  // 1. 验证输入
  const validated = kuakuaInputSchema.parse({ input })

  // 2. AI 处理
  const result = await aiService.generateKuakua(validated.input)

  // 3. 保存记录
  if (currentUser) {
    await db.kuakua.create({ ... })
  }

  // 4. 返回结果
  return result
}
```

### 4.3 中间件架构

```typescript
// middleware.ts
export function middleware(request: NextRequest) {
  // 1. 认证检查
  // 2. 速率限制
  // 3. 日志记录
  // 4. CORS 处理
}
```

---

## 五、数据库设计

### 5.1 核心数据表

**用户表 (users)**:
```sql
CREATE TABLE users (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  email VARCHAR(255) UNIQUE NOT NULL,
  password_hash VARCHAR(255) NOT NULL,
  nickname VARCHAR(50),
  avatar_url TEXT,
  subscription_type VARCHAR(20) DEFAULT 'free',
  created_at TIMESTAMP DEFAULT NOW(),
  updated_at TIMESTAMP DEFAULT NOW()
);
```

**夸夸记录表 (kuakua_logs)**:
```sql
CREATE TABLE kuakua_logs (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  input_text TEXT NOT NULL,
  sentiment VARCHAR(20),
  response_text TEXT NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);
```

**测试表 (tests)**:
```sql
CREATE TABLE tests (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  title VARCHAR(255) NOT NULL,
  description TEXT,
  category VARCHAR(50),
  questions JSONB NOT NULL,
  is_premium BOOLEAN DEFAULT false,
  created_at TIMESTAMP DEFAULT NOW()
);
```

**测试结果表 (test_results)**:
```sql
CREATE TABLE test_results (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES users(id),
  test_id UUID REFERENCES tests(id),
  answers JSONB NOT NULL,
  result JSONB NOT NULL,
  created_at TIMESTAMP DEFAULT NOW()
);
```

### 5.2 索引设计

```sql
-- 性能优化索引
CREATE INDEX idx_kuakua_user ON kuakua_logs(user_id);
CREATE INDEX idx_kuakua_created ON kuakua_logs(created_at);
CREATE INDEX idx_test_results_user ON test_results(user_id);
CREATE INDEX idx_test_results_test ON test_results(test_id);
CREATE INDEX idx_users_email ON users(email);
```

### 5.3 数据关系图

```
users (1) ──────< (N) kuakua_logs
  │
  ├─────────────< (N) test_results
  │
  └─ (1:N) ────> tests
```

---

## 六、AI 集成架构

### 6.1 AI 服务架构

```
┌─────────────────────────────────────────────────────┐
│                    应用层                            │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │  夸夸    │  │  测试    │  │  内容    │         │
│  └──────────┘  └──────────┘  └──────────┘         │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│                  AI 抽象层                           │
│  ┌──────────────────────────────────────────┐      │
│  │  AI Service (Singleton)                 │      │
│  │  - Prompt 管理                           │      │
│  │  - 缓存策略                              │      │
│  │  - 错误处理                              │      │
│  │  - 成本优化                              │      │
│  └──────────────────────────────────────────┘      │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│                AI Provider 层                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐         │
│  │ OpenAI   │  │  Claude  │  │  本地    │         │
│  │  GPT-4   │  │  (备用)  │  │  模型    │         │
│  └──────────┘  └──────────┘  └──────────┘         │
└─────────────────────────────────────────────────────┘
```

### 6.2 Prompt 管理策略

```typescript
// lib/ai/prompts.ts
export const PROMPTS = {
  kuakua: {
    system: "你是一个温暖、真诚的心理咨询师...",
    templates: {
      positive: "用户表达了积极情绪，请鼓励并强化...",
      negative: "用户表达了消极情绪，请共情并支持..."
    }
  },
  testAnalysis: {
    system: "你是一个专业的心理测试分析师...",
    template: "基于以下测试答案，生成个性化报告..."
  }
}
```

### 6.3 缓存策略

```typescript
// 相似输入复用结果
const cacheKey = `kuakua:${hash(input)}`
const cached = await redis.get(cacheKey)
if (cached) return cached

// 缓存 24 小时
await redis.setex(cacheKey, 86400, result)
```

---

## 七、安全架构

### 7.1 认证授权流程

```
┌─────────┐    注册/登录     ┌──────────┐
│  用户   │ ──────────────→ │  Next.js  │
└─────────┘                 └──────────┘
                                 │
                                 ↓
                         ┌──────────────┐
                         │  验证凭证     │
                         └──────────────┘
                                 │
                    ┌──────────────┼──────────────┐
                    ↓              ↓              ↓
              ┌─────────┐    ┌─────────┐    ┌─────────┐
              │ 检查用户 │    │ 密码验证 │    │ 生成JWT │
              └─────────┘    └─────────┘    └─────────┘
                    │              │              │
                    └──────────────┼──────────────┘
                                   ↓
                          ┌──────────────┐
                          │ 返回Token    │
                          │ 设置Cookie   │
                          └──────────────┘
```

### 7.2 RBAC 权限模型

```typescript
enum Role {
  GUEST = 'guest',      // 游客
  USER = 'user',        // 注册用户
  MEMBER = 'member',    // 会员
  ADMIN = 'admin'       // 管理员
}

interface Permission {
  resource: string     // 资源
  action: string       // 操作
  condition?: (user: User) => boolean
}

const permissions: Record<Role, Permission[]> = {
  guest: [
    { resource: 'kuakua', action: 'create', condition: u => u.dailyCount < 3 },
    { resource: 'tests', action: 'read' }
  ],
  user: [
    { resource: 'kuakua', action: 'create', condition: u => u.dailyCount < 3 },
    { resource: 'tests', action: ['read', 'create'] },
    { resource: 'profile', action: ['read', 'update'] }
  ],
  member: [
    { resource: 'kuakua', action: 'create' },  // 无限制
    { resource: 'tests', action: ['read', 'create'] },
    { resource: 'theories', action: 'read' }
  ],
  admin: [
    { resource: '*', action: '*' }
  ]
}
```

### 7.3 数据加密策略

| 数据类型 | 加密方式 | 说明 |
|---------|---------|------|
| 传输数据 | TLS 1.3 | HTTPS |
| 密码 | bcrypt | 10 轮加盐 |
| 敏感字段 | AES-256 | 应用层加密 |
| Token | HMAC-SHA256 | JWT 签名 |

---

## 八、部署架构

### 8.1 部署拓扑

```
┌─────────────────────────────────────────────────────┐
│                     用户                            │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│              EdgeOne CDN (全球节点)                  │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐          │
│  │ 静态资源 │  │ DDoS防护 │  │ WAF     │          │
│  └──────────┘  └──────────┘  └──────────┘          │
└─────────────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────────────┐
│            Vercel / EdgeOne Serverless              │
│  ┌──────────────────────────────────────────┐       │
│  │         Next.js 应用容器                 │       │
│  │  ┌──────────┐  ┌──────────┐            │       │
│  │  │ SSR页面  │  │ API函数  │            │       │
│  │  └──────────┘  └──────────┘            │       │
│  └──────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────┘
        ↓                       ↓
┌──────────────┐      ┌──────────────┐
│  Neon DB     │      │  OpenAI API  │
│  (PostgreSQL)│      │  (外部)      │
└──────────────┘      └──────────────┘
```

### 8.2 环境配置

```bash
# .env.production
DATABASE_URL=postgresql://...
NEXT_PUBLIC_URL=https://yourdomain.com
NEXT_PUBLIC_APP_NAME=心语AI

# Auth
AUTH_SECRET=your-secret-key
JWT_SECRET=your-jwt-secret

# AI
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4

# OAuth (可选)
GOOGLE_CLIENT_ID=...
GOOGLE_CLIENT_SECRET=...

# Analytics
NEXT_PUBLIC_GA_ID=G-...
```

### 8.3 CI/CD 流程

```
Git Push
   ↓
GitHub Actions / CNB
   ↓
┌──────────────┐
│  运行测试    │
│  lint check  │
│  type check  │
└──────────────┘
   ↓
┌──────────────┐
│  构建项目    │
│  bun run build│
└──────────────┘
   ↓
┌──────────────┐
│  部署到 EdgeOne│
│  海外节点    │
└──────────────┘
   ↓
自动验证部署
```

---

## 九、性能优化

### 9.1 前端优化

| 优化项 | 技术方案 | 预期效果 |
|--------|----------|----------|
| 代码分割 | Next.js 动态导入 | 减少初始加载 50% |
| 图片优化 | next/image | 减少 70% 图片大小 |
| 字体优化 | next/font | 0 闪烁 |
| 缓存策略 | SWR / ISR | 减少 API 调用 80% |
| 懒加载 | React.lazy | 加速首屏 30% |

### 9.2 后端优化

| 优化项 | 技术方案 | 预期效果 |
|--------|----------|----------|
| 数据库连接池 | Drizzle 连接池 | 提升 30% 查询速度 |
| 查询优化 | 索引 + 查询优化 | 减少 50% 查询时间 |
| 结果缓存 | Redis / Vercel KV | 减少 90% 重复计算 |
| API 响应压缩 | gzip / brotli | 减少 70% 传输大小 |

### 9.3 AI 优化

| 优化项 | 技术方案 | 预期效果 |
|--------|----------|----------|
| Prompt 优化 | Few-shot + Chain of Thought | 提升 20% 准确率 |
| 结果缓存 | 相似输入复用 | 减少 80% API 调用 |
| 模型选择 | GPT-3.5 + GPT-4 分层 | 减少 60% 成本 |
| 批处理 | 合并请求 | 提升 40% 效率 |

---

## 十、监控与日志

### 10.1 监控指标

**应用监控**:
```typescript
// Vercel Analytics
import { Analytics } from '@vercel/analytics/react'

<Analytics />

// 自定义事件
analytics.track('kuakua_created', {
  sentiment: 'positive',
  length: 50
})
```

**性能监控**:
```typescript
// Web Vitals
import { getCLS, getFID, getFCP } from 'web-vitals'

getCLS(console.log)
getFID(console.log)
getFCP(console.log)
```

### 10.2 日志策略

```typescript
// pino - 结构化日志
logger.info({
  event: 'kuakua_created',
  userId: user.id,
  sentiment: result.sentiment,
  duration: ms
})
```

### 10.3 错误追踪

```typescript
// Sentry
import * as Sentry from '@sentry/nextjs'

Sentry.init({
  dsn: process.env.SENTRY_DSN,
  environment: process.env.NODE_ENV
})
```

---

## 十一、扩展性设计

### 11.1 水平扩展

```
应用层 (无状态)
   ↓
负载均衡器
   ↓
┌──────────┬──────────┬──────────┐
│ 实例 1  │ 实例 2  │ 实例 3  │
└──────────┴──────────┴──────────┘
   ↓           ↓           ↓
      共享数据库 (Neon)
```

### 11.2 垂直扩展

```typescript
// 数据库读写分离
const readDB = neonPool.read
const writeDB = neonPool.write

// AI 服务集群
const aiServices = [
  new OpenAIService('key1'),
  new OpenAIService('key2'),
  new OpenAIService('key3')
]
```

---

## 十二、技术风险与应对

| 风险 | 影响 | 概率 | 应对措施 |
|------|------|------|----------|
| OpenAI 限流 | 高 | 中 | 多供应商 + 缓存 |
| 数据库性能 | 中 | 低 | 读写分离 + 索引优化 |
| 服务器成本 | 高 | 中 | 代码优化 + 按需扩展 |
| 安全漏洞 | 高 | 低 | 代码审查 + 渗透测试 |

---

## 十三、未来演进

### 13.1 V2.0 架构升级

**微服务化**:
```
┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│   Web 服务   │  │  AI 服务     │  │  支付服务    │
│   (Next.js)  │  │  (Python)    │  │  (Node.js)   │
└──────────────┘  └──────────────┘  └──────────────┘
       ↓                 ↓                 ↓
              API Gateway
                   ↓
              服务网格
```

**数据库分片**:
```
用户库 (Shard 1)  用户库 (Shard 2)  用户库 (Shard 3)
```

### 13.2 AI 能力升级

- 多模态：语音输入/输出
- 本地模型：隐私保护
- 联邦学习：个性化训练

---

## 十四、附录

### 14.1 术语表

| 术语 | 解释 |
|------|------|
| SSR | Server Side Rendering，服务端渲染 |
| CSR | Client Side Rendering，客户端渲染 |
| ISR | Incremental Static Regeneration，增量静态再生成 |
| RBAC | Role-Based Access Control，基于角色的访问控制 |

### 14.2 参考文档

- Next.js 文档: https://nextjs.org/docs
- Drizzle ORM: https://orm.drizzle.team
- Vercel AI SDK: https://sdk.vercel.ai
- shadcn/ui: https://ui.shadcn.com

---

**文档状态**: 🟢 草案
**维护人**: 架构团队
**审核状态**: 待审核
